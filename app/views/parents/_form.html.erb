<%= form_for("parent", :url => child_parent_path, :html => { :method => :patch }, :child_id => @child.id) do |f| %>
  <% if @parent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@parent.errors.count, "error") %> prohibited this parent from being saved:</h2>

      <ul>
      <% @parent.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 
  <div class="field">
    <%= f.label :PLastName %><br>
    <%= f.text_area :PLastName %>
  </div>
  <div class="field">
    <%= f.label :PFirstName %><br>
    <%= f.text_area :PFirstName %>
  </div>
  <div class="field">
    <%= f.label :Email %><br>
    <%= f.text_area :Email %>
  </div>
  <div class="field">
    <%= f.label :Phone %><br>
    <%= f.number_field :Phone %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
